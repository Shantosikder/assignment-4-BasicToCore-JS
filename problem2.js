//problem 2
function evenOdd(str) {
    if (str.length % 2 === 0) {
        return "even";
    } else {
        return "odd";
    }
}

/*
when % is used with 2, the number is even if the remainder is zero. Other wise, the number is odd.
 */

